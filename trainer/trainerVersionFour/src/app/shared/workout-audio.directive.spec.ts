import { WorkoutAudioDirective } from './workout-audio.directive';

describe('WorkoutAudioDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkoutAudioDirective();
    expect(directive).toBeTruthy();
  });
});
