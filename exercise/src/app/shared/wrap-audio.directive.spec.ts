import { WrapAudioDirective } from './wrap-audio.directive';

describe('WrapAudioDirective', () => {
  it('should create an instance', () => {
    const directive = new WrapAudioDirective();
    expect(directive).toBeTruthy();
  });
});
